extends layouts/login-layout.pug
block content
    .loginContainer 
        h1 로그인
        form(method="post")
            p.errorMessage.text-danger.font-weight-bold #{errorMessage}
            input(type="text", name="userID" placeholder="학번", value=userID, autocomplete="off", maxlength="8", required="")
            input(type="password", name="password" placeholder="비밀번호", required="")
            input(type="submit", value="로그인")
        a(href="/register") 계정이 없으신가요? 계정을 만드세요.
